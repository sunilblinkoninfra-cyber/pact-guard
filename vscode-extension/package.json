{
  "name": "pact-sentinel",
  "displayName": "Pact Sentinel",
  "description": "AI-powered security analyzer for Kadena Pact smart contracts",
  "version": "1.0.0",
  "publisher": "pact-sentinel",
  "icon": "images/icon.png",
  "engines": { "vscode": "^1.85.0" },
  "categories": ["Linters", "Other"],
  "keywords": ["pact", "kadena", "blockchain", "security", "smart-contract"],
  "activationEvents": ["onLanguage:pact", "workspaceContains:**/*.pact"],
  "main": "./out/extension.js",
  "contributes": {
    "languages": [{
      "id": "pact",
      "aliases": ["Pact", "pact"],
      "extensions": [".pact"],
      "configuration": "./language-configuration.json"
    }],
    "grammars": [{
      "language": "pact",
      "scopeName": "source.pact",
      "path": "./syntaxes/pact.tmLanguage.json"
    }],
    "commands": [
      {
        "command": "pact-sentinel.analyzeFile",
        "title": "Pact Sentinel: Analyze Current File",
        "icon": "$(shield)"
      },
      {
        "command": "pact-sentinel.analyzeWorkspace",
        "title": "Pact Sentinel: Analyze Workspace",
        "icon": "$(search)"
      },
      {
        "command": "pact-sentinel.showReport",
        "title": "Pact Sentinel: Show Security Report"
      },
      {
        "command": "pact-sentinel.openSettings",
        "title": "Pact Sentinel: Settings"
      }
    ],
    "menus": {
      "editor/title": [{
        "command": "pact-sentinel.analyzeFile",
        "when": "resourceExtname == .pact",
        "group": "navigation"
      }],
      "explorer/context": [{
        "command": "pact-sentinel.analyzeFile",
        "when": "resourceExtname == .pact"
      }]
    },
    "configuration": {
      "title": "Pact Sentinel",
      "properties": {
        "pact-sentinel.pythonPath": {
          "type": "string",
          "default": "python3",
          "description": "Path to Python interpreter"
        },
        "pact-sentinel.sentinelPath": {
          "type": "string",
          "default": "",
          "description": "Path to pact-sentinel cli.py (leave blank to auto-detect)"
        },
        "pact-sentinel.anthropicApiKey": {
          "type": "string",
          "default": "",
          "description": "Anthropic API key for AI enrichment"
        },
        "pact-sentinel.enableAI": {
          "type": "boolean",
          "default": false,
          "description": "Enable AI-powered analysis (requires API key)"
        },
        "pact-sentinel.severityThreshold": {
          "type": "string",
          "enum": ["critical", "high", "medium", "low"],
          "default": "medium",
          "description": "Minimum severity to show diagnostics"
        },
        "pact-sentinel.analyzeOnSave": {
          "type": "boolean",
          "default": true,
          "description": "Automatically analyze .pact files on save"
        },
        "pact-sentinel.skipRules": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Rule IDs to skip (e.g. [\"R-003\", \"R-009\"])"
        }
      }
    },
    "views": {
      "explorer": [{
        "id": "pactSentinelResults",
        "name": "Security Findings",
        "when": "pact-sentinel.hasFindings"
      }]
    },
    "viewsContainers": {
      "activitybar": [{
        "id": "pact-sentinel",
        "title": "Pact Sentinel",
        "icon": "$(shield)"
      }]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/vscode": "^1.85.0",
    "@types/node": "^18.0.0",
    "typescript": "^5.0.0"
  }
}
